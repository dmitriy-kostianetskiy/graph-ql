type Book {
  title: String
  author: String
}

type Query {
  books: [Book]
  bookById(id: ID!): Book
}

input CreateBookInput {
  title: String!
  author: String!
}

type Mutation {
  createBook(id: ID!, book: CreateBookInput): Book
}
